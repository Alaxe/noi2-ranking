#include  <bits/stdc++.h>

using namespace std;
int main()
{
    int a;
    cin>>a;

    if ( a==1 ) { cout<<1<<endl; }
    if ( a==2 ) { cout<<3<<endl; }
    if ( a==3 ) { cout<<7<<endl; }
    if ( a==4 ) { cout<<13<<endl; }
    if ( a==5 ) { cout<<21<<endl; }
    if ( a==6 ) { cout<<31<<endl; }
    if ( a==7 ) { cout<<43<<endl; }
    if ( a==8 ) { cout<<57<<endl; }
    if ( a==9 ) { cout<<73<<endl; }

    return 0;
}
