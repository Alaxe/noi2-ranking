#include<iostream>
using namespace std;

int main()
{
	int N,K;
	do{cin>>N;}while(N<2 || N>5000);
	do{cin>>K;}while(K<1;k>550);
	int i,g[N],j,k[N][N];
	int max[N],count[N];
	for(i=0;i<N;i++){
		cin>>g[i]; 
		for(j=0;j<g;j++)cin>>k[i][j];
	}
	
	int q=0,cifra;
	for(j=0;j<g[q];j++){
		cifra=k[i]
	for(i=0;i<N;i++)
	{
		if(k[i][j]==cifra)
	} 
}
	return 0;
}
