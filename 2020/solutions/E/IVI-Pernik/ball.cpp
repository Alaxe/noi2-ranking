#include <iostream>
using namespace std;
int main()
{
    long long n,x,a=0,b=0,c=0;
    cin>>n>>x;
    if(x == 1)a=x;
    else if(x == 2)b=x;
    else c=x;
    for(int i=1;i <= n;n--)
    {
        if(n%2 == 0)
        {
            switch (b,c);
        }
        else
        {
            switch (a,b);
        }
    }
    if(a != 0)cout<<0<<endl;
    else if(b != 0)cout<<1<<endl;
    else cout<<2<<endl;
    return 0;
}
