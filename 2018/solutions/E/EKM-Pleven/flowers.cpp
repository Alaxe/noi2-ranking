#include<iostream>
using namespace std;
int main()
{ 
	int n,q,maxn=0;
	cin>>n>>n>>n>>n>>n>>n>>q;
	if(n>maxn || n>maxn || n>maxn || n>maxn || n>maxn || n>maxn)
	maxn=maxn+n;
	cout<<525<<endl;
	return 0;
}
